apiVersion: v1
kind: Service
metadata:
  name: kepler-api
  labels:
    app: kepler-engine
    component: api
spec:
  type: ClusterIP
  selector:
    app: kepler-engine
    component: api
  ports:
    - name: http
      port: 80
      targetPort: http
